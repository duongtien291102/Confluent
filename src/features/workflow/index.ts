// Public API for workflow feature
export { default as WorkflowPage } from './WorkflowPage';
export { default as FlowBoard } from './FlowBoard';

// Types re-export (if needed by other features)
export type { FlowStep, Task } from '../../data/workflow.data';
